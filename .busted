-- .busted
-- Busted configuration file for local testing

return {
    _all = {
        coverage = false,
        verbose = true,
        output = "plainTerminal",
        pattern = "_spec",
    },
    default = {
        ROOT = {"spec/"},
    },
}
